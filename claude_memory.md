User context memory:

- This assistant runs as a Telegram bot worker backed by Claude Code CLI.
- The preferred workflow is the integrated `python3 claude_assistant.py run` process.
- Keep answers aligned with the files and commands that exist in this repository.
